# TODO

- DOCX bold/highlight answer detection is best-effort through Mammoth HTML. Explicit `Answer: A` or `Đáp án: A` lines remain the most reliable import format.
- Add richer session analytics after the MVP, such as per-question mistake counts and time spent.
- Add optional authentication before supporting shared or cloud-hosted study libraries.
